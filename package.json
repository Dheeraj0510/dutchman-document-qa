{
  "name": "doc-app-api",
  "version": "1.0.0",
  "private": true,
  "description": "Minimal document upload and question API",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "vectors:up": "docker compose up -d qdrant",
    "test": "node --test",
    "test:integration": "vitest run test/integration.test.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "better-sqlite3": "^13.0.3",
    "dotenv": "^17.4.2",
    "express": "^5.1.0",
    "express-rate-limit": "^8.7.0",
    "mammoth": "^1.12.2",
    "multer": "^2.0.2",
    "pdf-parse": "^2.4.5"
  },
  "devDependencies": {
    "supertest": "^7.2.2",
    "vitest": "^4.1.11"
  }
}
